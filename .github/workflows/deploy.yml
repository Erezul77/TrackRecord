name: Deploy to DigitalOcean

# Runs on every push to main branch
on:
  push:
    branches:
      - main

jobs:
  deploy-api:
    runs-on: ubuntu-latest
    name: Deploy API
    
    steps:
      - name: Deploy API to Main Droplet
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DO_HOST }}
          username: ${{ secrets.DO_USERNAME }}
          key: ${{ secrets.DO_SSH_KEY }}
          script: |
            cd ~/TrackRecord
            
            echo "📥 Pulling latest code..."
            git pull origin main
            
            echo "🛑 Stopping old API container..."
            docker stop trackrecord-api || true
            docker rm trackrecord-api || true
            
            echo "🔨 Building API image..."
            docker build -t trackrecord-api ./backend
            
            echo "🚀 Starting API container..."
            docker run -d \
              --name trackrecord-api \
              -p 8000:8000 \
              --env-file backend/.env \
              -e GOOGLE_CLIENT_ID="${{ secrets.GOOGLE_CLIENT_ID }}" \
              -e GOOGLE_CLIENT_SECRET="${{ secrets.GOOGLE_CLIENT_SECRET }}" \
              -e ANTHROPIC_API_KEY="${{ secrets.ANTHROPIC_API_KEY }}" \
              --restart unless-stopped \
              trackrecord-api
            
            echo "✅ API Deployment complete!"
            docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"

  deploy-worker:
    runs-on: ubuntu-latest
    name: Deploy Worker
    if: ${{ vars.WORKER_ENABLED == 'true' }}
    
    steps:
      - name: Deploy Worker to Worker Droplet
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DO_WORKER_HOST }}
          username: ${{ secrets.DO_USERNAME }}
          key: ${{ secrets.DO_SSH_KEY }}
          script: |
            cd ~/TrackRecord || git clone https://github.com/Erezul77/TrackRecord.git ~/TrackRecord
            cd ~/TrackRecord
            
            echo "📥 Pulling latest code..."
            git pull origin main
            
            echo "🛑 Stopping old worker..."
            docker stop trackrecord-worker || true
            docker rm trackrecord-worker || true
            
            echo "🔨 Building Worker image..."
            docker build -t trackrecord-worker -f ./backend/Dockerfile.worker ./backend
            
            echo "🚀 Starting Worker container..."
            docker run -d \
              --name trackrecord-worker \
              --env-file backend/.env \
              -e ANTHROPIC_API_KEY="${{ secrets.ANTHROPIC_API_KEY }}" \
              -e RSS_INTERVAL_MINUTES=180 \
              -e RESOLUTION_INTERVAL_MINUTES=240 \
              -e HISTORICAL_ENABLED=true \
              --restart unless-stopped \
              trackrecord-worker
            
            echo "✅ Worker Deployment complete!"
            docker ps --format "table {{.Names}}\t{{.Status}}"
